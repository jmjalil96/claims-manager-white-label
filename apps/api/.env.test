# Test environment configuration
NODE_ENV=test

# Test database - separate from development
DATABASE_URL="postgresql://claims_user:claims_password@localhost:5433/claims_test"

# Auth (same secrets are fine for tests)
BETTER_AUTH_SECRET="test-secret-key"
BETTER_AUTH_URL="http://localhost:3001"

# Claims
CLAIM_NUMBER_SALT="test-salt"
