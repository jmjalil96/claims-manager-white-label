/**
 * Data Transfer Objects for list claims endpoint
 * Re-exported from @claims/shared for consistency
 */

export type { ClaimListItemDto, ListClaimsResponse, PaginationMeta } from '@claims/shared'
